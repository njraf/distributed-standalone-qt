#include <QtCore>

class Counter
{
    // Replicated state — automatically synced from source -> all replicas
    PROP(int count = 0);

    // Called on the client, executed on the server
    SLOT(void increment());

};
